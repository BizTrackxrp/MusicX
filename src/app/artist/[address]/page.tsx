import ArtistPage from './ArtistPage';

export default function Page() {
  return <ArtistPage />;
}
```

**5. Click "Commit changes"**

---

**6. Then do it again - "Add file" → "Create new file"**

**7. In the filename box, type:**
```
artist/[address]/ArtistPage.tsx
